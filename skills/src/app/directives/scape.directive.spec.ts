import { ScapeDirective } from './scape.directive';

describe('ScapeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScapeDirective();
    expect(directive).toBeTruthy();
  });
});
